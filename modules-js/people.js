const people = ['yoshi', 'ryu', 'chun-li', 'mario'];
const ages = [20, 30, 40, 50];

console.log(people);

module.exports = {
    people, ages
}

// on peut importer le people.js dans modules en utilisant le require  